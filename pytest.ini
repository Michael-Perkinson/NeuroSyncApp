[pytest]
pythonpath = .
python_files = test_*.py
testpaths = tests
markers =
    integration: marks tests as integration tests (may require example data files)
    unit: marks tests as unit tests (pure logic, no file I/O)